apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: prisma-docker-test
  namespace: default
spec:
  imageRepositoryRef:
    name: prisma-docker-test
  policy:
    semver:
      range: 0.0.x
apiVersion: v1
kind: Service
metadata:
  namespace: default
  name: prisma-docker-test
  labels:
    app: prisma-docker-test
spec:
  type: ClusterIP
  selector:
    app: prisma-docker-test
  ports:
  - protocol: TCP
    name: http
    port: 80
    targetPort: 3000
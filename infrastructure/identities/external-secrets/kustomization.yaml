apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: identities
  namespace: default
spec:
  interval: 15m
  path: "./infrastructure/identities/external-secrets/deploy"
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system